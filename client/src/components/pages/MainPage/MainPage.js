import * as React from 'react';
import Header from '@/components/atoms/Header';

const MainPage = (): React.ComponentType<Object> => (
  <div>
    <Header>Hello World React!</Header>
    <div>This is the content</div>
  </div>
);

export default MainPage;
